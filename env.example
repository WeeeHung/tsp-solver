# Google Maps API Configuration
# Get your API key from: https://console.cloud.google.com/google/maps-apis/

# Required for:
# - Distance Matrix API (driving distance calculation)
# - Directions API (route visualization with actual roads)
GOOGLE_MAPS_API_KEY=your_api_key_here

# Note: 
# - Copy this file to .env and replace 'your_api_key_here' with your actual API key
# - The .env file is gitignored for security
# - If no API key is provided, the system will fall back to geodesic (straight-line) distance

